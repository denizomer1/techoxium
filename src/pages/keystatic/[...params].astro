---
const { makeHandler } = await import('@keystatic/astro')
const keystaticHandler = makeHandler({
  config: (await import('../../../keystatic.config')).default,
})

export const ALL = keystaticHandler
---
